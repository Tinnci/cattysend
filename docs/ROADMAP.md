# 🗺️ Cattysend 开发路线图

> **互传联盟协议 Linux 实现** - 从零到生产级的完整旅程

[![项目状态](https://img.shields.io/badge/status-active_development-brightgreen)]()
[![版本](https://img.shields.io/badge/version-0.1.0--alpha-blue)]()
[![协议兼容](https://img.shields.io/badge/protocol_compat-100%25-success)]()

---

## 📊 项目状态总览

<table>
<thead>
  <tr>
    <th>阶段</th>
    <th>里程碑</th>
    <th>状态</th>
    <th>完成度</th>
    <th>预计完成</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>Phase 1</td>
    <td>核心协议</td>
    <td>✅ 完成</td>
    <td>100%</td>
    <td>2026-01</td>
  </tr>
  <tr>
    <td>Phase 2</td>
    <td>CLI/Daemon 架构</td>
    <td>✅ 完成</td>
    <td>100%</td>
    <td>2026-01</td>
  </tr>
  <tr>
    <td>Phase 3</td>
    <td>TUI 交互界面</td>
    <td>✅ 完成</td>
    <td>100%</td>
    <td>2026-01</td>
  </tr>
  <tr>
    <td>Phase 4</td>
    <td>真实 BLE 集成</td>
    <td>🔄 进行中</td>
    <td>85%</td>
    <td>2026-02</td>
  </tr>
  <tr>
    <td>Phase 5</td>
    <td>NM D-Bus 集成</td>
    <td>🔄 进行中</td>
    <td>75%</td>
    <td>2026-03</td>
  </tr>
  <tr>
    <td>Phase 6</td>
    <td>GUI 托盘应用</td>
    <td>⏳ 计划中</td>
    <td>0%</td>
    <td>2026-04</td>
  </tr>
</tbody>
</table>

### 整体进度

```
[████████████████████████████░░░░] 83% 完成
```

**当前状态**: 🚀 可用于日常文件传输，正在优化用户体验

---

## Phase 1: 核心协议 ✅

**目标**: 实现与 CatShare (Android) 完全兼容的协议层

### 已完成
- [x] BLE UUID 常量定义
- [x] DeviceInfo 结构体 (state/key/mac/catShare)
- [x] P2pInfo 结构体 (id/ssid/psk/mac/port/key/catShare)
- [x] ECDH (P-256) 密钥交换
- [x] AES-256-CTR 加密/解密
- [x] 正确的 IV: ASCII `"0102030405060708"`
- [x] 直接使用 ECDH 共享密钥 (无 HKDF)

### 测试覆盖
- [x] 加密/解密往返测试
- [x] IV 格式验证测试

---

## Phase 2: CLI/Daemon 架构 ✅

**目标**: 实现守护进程 + CLI 客户端模式

### 已完成
- [x] Cargo Workspace 结构
- [x] `cattysend-core` 核心库
- [x] `cattysend-daemon` systemd 服务
- [x] `cattysend-cli` clap 命令行
- [x] Unix Domain Socket IPC
- [x] `xtask` 自动化工具

### xtask 命令
| 命令 | 功能 |
|------|------|
| `cargo xtask build` | 构建 release 版本 |
| `cargo xtask install` | 安装 systemd 服务 |
| `cargo xtask uninstall` | 卸载服务 |
| `cargo xtask setup-caps` | 设置免 sudo 权限 |
| `cargo xtask dist` | 打包发布 |

---

## Phase 3: TUI 交互界面 🔄

**目标**: 使用 ratatui 提供交互式终端体验

### 已完成 ✅
- [x] 设备列表面板 (真实 BLE 数据)
- [x] 智能设备名称解析 (Manufacturer Data/Service Data)
- [x] 日志面板 (自动换行，多级过滤)
- [x] Tab 页切换 (设备/传输/日志)
- [x] 键盘快捷键
- [x] 命令行文件参数支持

### 待完成 🔄
- [ ] 交互式文件选择器
- [ ] RSSI 信号强度条显示 (目前仅文本)
- [ ] 传输进度条联动
- [ ] 接收模式 TUI 集成

### 快捷键
| 按键 | 功能 |
|------|------|
| `s` | 扫描设备 |
| `r` | 接收模式 |
| `↑/↓` | 选择设备 |
| `Enter` | 选中并发送 (若指定文件) |
| `Tab` | 切换标签 |
| `q` | 退出 |

---

## Phase 4: 真实 BLE 集成 🔄

**目标**: 连接真实的 BLE 硬件

### 已完成 ✅
- [x] BLE 扫描集成 (btleplug)
  - `BleScanner`: 扫描带有 SERVICE_UUID 的设备
  - `DiscoveredDevice`: 设备名称、地址、RSSI、品牌 ID
- [x] GATT 服务发现
- [x] CHAR_STATUS 读取
  - 读取 DeviceInfo JSON，包含对方公钥
- [x] CHAR_P2P 写入
  - 加密 P2pInfo 并写入
- [x] BLE 广播 (bluer GATT Server)
  - `GattServer`: 完整的 GATT Server 实现
  - STATUS 特征只读，返回 DeviceInfo
  - P2P 特征可写，接收加密的 P2pInfo
- [x] 设备过滤 (Service UUID `00003331-...`)
- [x] BLE 客户端 (`BleClient`)
  - 连接到接收端
  - 读取 DeviceInfo
  - ECDH 密钥协商
  - 加密并写入 P2pInfo

### 待完成
- [ ] 完整的端到端测试 (需要硬件)
- [ ] 错误恢复和重试机制
- [ ] 连接状态监控

### 技术挑战
- BlueZ 权限配置 (`CAP_NET_ADMIN`)
- D-Bus 会话管理
- 跨发行版兼容性

---

## Phase 5: WiFi P2P 实现 🔄

**目标**: 使用 wpa_supplicant 创建/加入 P2P 组

### 已完成 ✅
- [x] WiFi P2P 发送端 (`WiFiP2pSender`)
  - wpa_cli P2P group 创建
  - nmcli 热点备用方案
  - 随机 SSID/PSK 生成
  - MAC 地址获取
- [x] WiFi P2P 接收端 (`WiFiP2pReceiver`)  
  - wpa_cli 连接
  - nmcli 连接备用
  - IP 地址获取

### 待完成
- [ ] **NetworkManager D-Bus 原生集成** (替代 `nmcli` 子进程)
  - 使用 `zbus` 或 `dbus-rs` 直接与 NM 守护进程通信
  - 实现状态变化的实时监听 (Signal 监听)
- [ ] **P2P 并发配置探索**
  - 研究通过 NM 配置实现 Station + P2P 共存
  - 避免 P2P 启动时不仅断开主网络
- [ ] DHCP 服务器配置 (dnsmasq)

### 依赖
- `NetworkManager` 1.16+
- `libnm` (可选开发依赖)
- `CAP_NET_ADMIN` (由 NM PolicyKit 代理，应用层无需直接持有)

---

## Phase 6: GUI 托盘应用 ⏳

**目标**: 提供类似 AirDrop 的桌面体验

### 方案选择
| 框架 | 优势 | 适用场景 |
|------|------|----------|
| **Slint** | 轻量、原生渲染 | 托盘常驻 |
| **Tauri** | WebView、生态丰富 | 复杂 UI |
| **Iced** | 纯 Rust、Elm 架构 | 性能极致 |

### 待完成
- [ ] 系统托盘图标
- [ ] 桌面通知
- [ ] 拖拽发送
- [ ] 文件接收确认弹窗
- [ ] 设置界面
- [ ] 暗色/亮色主题

---

## 文件传输协议 ✅

**目标**: 与 CatShare 完全兼容的 HTTP/WebSocket 传输

### 已完成
- [x] WebSocket 消息协议 (`type:id:name?payload` 格式)
  - `WsMessage` 结构体
  - 版本协商 (`versionNegotiation`)
  - 发送请求 (`sendRequest`)
  - 状态更新 (`status`)
- [x] 传输服务器 (`TransferServer`)
  - HTTP/HTTPS 服务
  - WebSocket 处理
  - ZIP 文件打包
- [x] 接收客户端 (`ReceiverClient`)
  - TLS 连接 (跳过证书验证)
  - WebSocket 协商
  - ZIP 解压

### 待完成
- [ ] HTTPS 证书生成
- [ ] 传输进度回调
- [ ] 断点续传

---

## 技术债务 & 改进

### 当前问题
1. ~~**TUI 设备模拟**: 当前使用模拟数据，需连接真实 BLE~~ ✅ 已实现 BLE 扫描
2. ~~**P2P 热点**: 当前为模拟实现，需 wpa_supplicant 集成~~ ✅ 已实现
3. **错误处理**: 部分错误直接 panic，需改为优雅降级

### 代码质量
- [ ] 添加更多单元测试
- [ ] 集成测试 (需要硬件)
- [x] API 文档 (rustdoc)
- [ ] 示例代码

---


---

## 📚 技术债务 & 改进计划

### 🐛 当前已知问题

| 优先级 | 问题 | 影响范围 | 计划 |
|--------|------|----------|------|
| 🔴 高 | NetworkManager P2P 并发限制 | WiFi 用户体验 | Phase 5 解决 |
| 🟡 中 | 部分 BlueZ 版本兼容性 | 少数发行版 | 持续测试 |
| 🟢 低 | 代码覆盖率 \< 100% | 维护性 | 持续改进 |

### 🎯 优化计划

#### Q1 2026 (当前)
- ✅ ~~完成核心协议实现~~
- 🔄 优化 WiFi P2P 体验
- 🔄 提升错误处理机制
- ⏳ 编写用户手册

#### Q2 2026
- [ ] GUI 托盘应用 MVP
- [ ] 支持更多设备型号
- [ ] 性能优化 (内存、速度)
- [ ] 国际化支持

---

## 🤝 贡献指南

### 开发环境设置

#### 1. 克隆项目

```bash
git clone https://github.com/user/cattysend
cd cattysend
```

#### 2. 安装依赖

**Rust 工具链** (最低 1.75+):
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
rustup toolchain install stable
```

**系统依赖** (Ubuntu/Debian):
```bash
sudo apt install -y \
    libdbus-1-dev \
    libbluetooth-dev \
    pkg-config \
    libssl-dev \
    networkmanager
```

**系统依赖** (Fedora/RHEL):
```bash
sudo dnf install -y \
    dbus-devel \
    bluez-libs-devel \
    pkgconfig \
    openssl-devel \
    NetworkManager
```

#### 3. 构建项目

```bash
# 开发模式 (快速编译)
cargo build

# 发布模式 (优化性能)
cargo xtask build
```

#### 4. 运行测试

```bash
# 所有单元测试
cargo test --workspace

# 特定模块测试
cargo test -p cattysend-core

# 带详细输出
cargo test -- --nocapture
```

### 开发工作流

#### 启动 TUI 调试模式

```bash
# 方法 1: 使用 xtask (推荐)
cargo xtask tui --log-level debug --log-file /tmp/cattysend.log

# 方法 2: 直接运行
RUST_LOG=debug cargo run -p cattysend-tui 2>> /tmp/cattysend.log

# 实时查看日志
tail -f /tmp/cattysend.log
```

#### 代码检查

```bash
# 格式化代码
cargo fmt --all

# Lint 检查
cargo clippy --all-targets --all-features -- -D warnings

# 安全审计
cargo audit
```

### 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 格式：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型 (type)**:
- `feat`: 新功能
- `fix`: 修复 bug
- `refactor`: 代码重构
- `docs`: 文档更新
- `test`: 测试相关
- `chore`: 构建工具、依赖更新
- `perf`: 性能优化
- `style`: 代码风格调整

**示例**:
```bash
feat(ble): add support for multiple adapters

- Detect all available BLE adapters
- Allow user to select adapter in TUI
- Update scanner to use selected adapter

Closes #42
```

### Pull Request 流程

1. **Fork 项目** 并创建特性分支
   ```bash
   git checkout -b feat/amazing-feature
   ```

2. **实现功能** 并编写测试
   ```bash
   # 编写代码...
   cargo test
   ```

3. **提交变更**
   ```bash
   git add .
   git commit -m "feat(module): add amazing feature"
   ```

4. **推送到你的 fork**
   ```bash
   git push origin feat/amazing-feature
   ```

5. **创建 Pull Request** 并填写 PR 模板

### 代码审查标准

✅ **必须满足**:
- 所有测试通过 (`cargo test`)
- 无 Clippy 警告 (`cargo clippy`)
- 代码已格式化 (`cargo fmt`)
- 添加必要的文档注释
- 更新相关 README

🎯 **建议满足**:
- 添加单元测试 (覆盖新代码)
- 更新 CHANGELOG.md
- 提供示例代码

---

## 🏗️ 架构图

### 模块层次结构

```
┌─────────────────────────────────────────────────────────────┐
│                     应用层 (Applications)                    │
├──────────────┬──────────────┬──────────────┬────────────────┤
│ cattysend-cli│ cattysend-tui│cattysend-daemon│ (future GUI) │
│              │              │              │                │
│  - clap      │  - ratatui   │  - systemd   │  - slint?      │
│  - IPC       │  - crossterm │  - tokio     │  - tauri?      │
└──────────────┴──────────────┴──────────────┴────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  核心库 (cattysend-core)                     │
├─────────────┬─────────────┬─────────────┬──────────────────┤
│     ble     │   crypto    │    wifi     │    transfer      │
├─────────────┼─────────────┼─────────────┼──────────────────┤
│ • Scanner   │ • ECDH      │ • P2P       │ • HTTP Server    │
│ • Client    │   (P-256)   │   Sender    │ • WS Handler     │
│ • Server    │ • AES-CTR   │ • P2P       │ • Receiver       │
│ • Advertiser│ • Session   │   Receiver  │ • File Stream    │
└─────────────┴─────────────┴─────────────┴──────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    系统层 (System Layer)                     │
├───────────────────┬─────────────────┬───────────────────────┤
│   BLE (BlueZ)     │  WiFi (NM/WPA)  │   Network (Tokio)     │
│                   │                 │                       │
│ • btleplug        │ • nmcli         │ • axum                │
│ • bluer           │ • wpa_cli       │ • tokio-tungstenite   │
│ • D-Bus           │ • D-Bus         │ • reqwest             │
└───────────────────┴─────────────────┴───────────────────────┘
```

### 数据流向

```
发送端流程:
┌──────────┐   BLE    ┌──────────┐  WiFi P2P  ┌──────────┐  HTTP/WS  ┌──────────┐
│  Sender  │ ────────►│ Receiver │ ──────────►│  P2P GO  │ ─────────►│ Transfer │
│  (TUI)   │ Handshake│  Device  │  Connect   │ (Hotspot)│ File Data │ Complete │
└──────────┘          └──────────┘            └──────────┘           └──────────┘
     │                      │                       │                      │
     │ 1. Scan              │ 3. Send P2P Info      │ 5. HTTP GET          │
     │ 2. ECDH              │ 4. Connect WiFi       │ 6. Download          │
     └──────────────────────┴───────────────────────┴──────────────────────┘
```

---

## 🌟 项目里程碑

### v0.1.0-alpha (当前)
- [x] 核心协议实现
- [x] BLE 扫描与 GATT
- [x] 基础 WiFi P2P
- [x] TUI 界面
- [ ] 稳定性测试

### v0.2.0-beta (Q2 2026)
- [ ] WiFi 并发优化
- [ ] GUI 托盘应用
- [ ] 自动更新机制
- [ ] 完整用户手册

### v1.0.0 (Q3 2026)
- [ ] 生产级稳定性
- [ ] 多设备支持
- [ ] 性能优化
- [ ] 国际化
- [ ] 官方发布

---

## 📞 联系方式

- **GitHub Issues**: [项目 GitHub](https://github.com/user/cattysend/issues)
- **讨论区**: [GitHub Discussions](https://github.com/user/cattysend/discussions)
- **邮件列表**: cattysend-dev@groups.io (计划中)

---

## 📜 许可证

本项目采用 **MIT License** 开源。详见 [LICENSE](../LICENSE) 文件。

---

**文档版本**: 2.0  
**最后更新**: 2026-01-20  
**维护者**: Cattysend 开发团队

